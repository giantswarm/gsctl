# This YAML file is a minimal v4 specification for a cluster
# to be passed via the `gsctl create cluster -f` command.
# All ommitted specifications will be set via defaults.
name: Minimal cluster spec
owner: myorg
